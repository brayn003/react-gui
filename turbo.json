{
    "$schema": "https://turborepo.org/schema.json",
    "baseBranch": "origin/master",
    "pipeline": {
        "dev": {
            "dependsOn": ["^dev"],
            "outputs": ["dist/**", ".next/**"],
            "inputs": ["src/**/*.tsx", "src/**/*.ts", "test/**/*.ts"]
        }
    }
}